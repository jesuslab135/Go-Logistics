// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: fuel_comment.sql

package gen

import (
	"context"
	"time"
)

const countFuelComments = `-- name: CountFuelComments :one
SELECT count(*) FROM fuel_comment c JOIN fuel_entry p0 ON p0.id = c.entry_id JOIN asset p1 ON p1.id = p0.asset_id WHERE c.entry_id = $1 AND p1.company_id = $2
`

type CountFuelCommentsParams struct {
	ParentID  int64
	CompanyID int64
}

func (q *Queries) CountFuelComments(ctx context.Context, arg CountFuelCommentsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countFuelComments, arg.ParentID, arg.CompanyID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createFuelComment = `-- name: CreateFuelComment :one
INSERT INTO fuel_comment (
    entry_id, employee_id, text, created_at, updated_at
)
SELECT $1, $2, $3, $4, $5
WHERE EXISTS (SELECT 1 FROM fuel_entry p0 JOIN asset p1 ON p1.id = p0.asset_id WHERE p0.id = $1 AND p1.company_id = $6)
RETURNING id, entry_id, employee_id, text, created_at, updated_at
`

type CreateFuelCommentParams struct {
	ParentID   int64
	EmployeeID *int64
	Text       string
	CreatedAt  time.Time
	UpdatedAt  time.Time
	CompanyID  int64
}

func (q *Queries) CreateFuelComment(ctx context.Context, arg CreateFuelCommentParams) (FuelComment, error) {
	row := q.db.QueryRow(ctx, createFuelComment,
		arg.ParentID,
		arg.EmployeeID,
		arg.Text,
		arg.CreatedAt,
		arg.UpdatedAt,
		arg.CompanyID,
	)
	var i FuelComment
	err := row.Scan(
		&i.ID,
		&i.EntryID,
		&i.EmployeeID,
		&i.Text,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteFuelComment = `-- name: DeleteFuelComment :exec
DELETE FROM fuel_comment AS c USING fuel_entry p0, asset p1
WHERE c.id = $1 AND c.entry_id = $2 AND p1.company_id = $3 AND p0.id = c.entry_id AND p1.id = p0.asset_id
`

type DeleteFuelCommentParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) DeleteFuelComment(ctx context.Context, arg DeleteFuelCommentParams) error {
	_, err := q.db.Exec(ctx, deleteFuelComment, arg.ID, arg.ParentID, arg.CompanyID)
	return err
}

const getFuelComment = `-- name: GetFuelComment :one
SELECT c.id, c.entry_id, c.employee_id, c.text, c.created_at, c.updated_at FROM fuel_comment c JOIN fuel_entry p0 ON p0.id = c.entry_id JOIN asset p1 ON p1.id = p0.asset_id
WHERE c.id = $1 AND c.entry_id = $2 AND p1.company_id = $3
`

type GetFuelCommentParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) GetFuelComment(ctx context.Context, arg GetFuelCommentParams) (FuelComment, error) {
	row := q.db.QueryRow(ctx, getFuelComment, arg.ID, arg.ParentID, arg.CompanyID)
	var i FuelComment
	err := row.Scan(
		&i.ID,
		&i.EntryID,
		&i.EmployeeID,
		&i.Text,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listFuelComments = `-- name: ListFuelComments :many
SELECT c.id, c.entry_id, c.employee_id, c.text, c.created_at, c.updated_at FROM fuel_comment c JOIN fuel_entry p0 ON p0.id = c.entry_id JOIN asset p1 ON p1.id = p0.asset_id
WHERE c.entry_id = $1 AND p1.company_id = $2
ORDER BY c.created_at DESC LIMIT $4 OFFSET $3
`

type ListFuelCommentsParams struct {
	ParentID  int64
	CompanyID int64
	Off       int32
	Lim       int32
}

func (q *Queries) ListFuelComments(ctx context.Context, arg ListFuelCommentsParams) ([]FuelComment, error) {
	rows, err := q.db.Query(ctx, listFuelComments,
		arg.ParentID,
		arg.CompanyID,
		arg.Off,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FuelComment{}
	for rows.Next() {
		var i FuelComment
		if err := rows.Scan(
			&i.ID,
			&i.EntryID,
			&i.EmployeeID,
			&i.Text,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateFuelComment = `-- name: UpdateFuelComment :one
UPDATE fuel_comment AS c SET text = $1, updated_at = $2
FROM fuel_entry p0, asset p1
WHERE c.id = $3 AND c.entry_id = $4 AND p1.company_id = $5 AND p0.id = c.entry_id AND p1.id = p0.asset_id
RETURNING c.id, c.entry_id, c.employee_id, c.text, c.created_at, c.updated_at
`

type UpdateFuelCommentParams struct {
	Text      string
	UpdatedAt time.Time
	ID        int64
	ParentID  int64
	CompanyID int64
}

// Authorship is stamped once at creation and never reassigned by an edit.
func (q *Queries) UpdateFuelComment(ctx context.Context, arg UpdateFuelCommentParams) (FuelComment, error) {
	row := q.db.QueryRow(ctx, updateFuelComment,
		arg.Text,
		arg.UpdatedAt,
		arg.ID,
		arg.ParentID,
		arg.CompanyID,
	)
	var i FuelComment
	err := row.Scan(
		&i.ID,
		&i.EntryID,
		&i.EmployeeID,
		&i.Text,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
