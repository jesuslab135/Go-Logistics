// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: tire_installation.sql

package gen

import (
	"context"
	"time"

	"github.com/shopspring/decimal"
)

const countTireInstallations = `-- name: CountTireInstallations :one
SELECT count(*) FROM tire_installation c JOIN tire p ON p.id = c.tire_id WHERE c.tire_id = $1 AND p.company_id = $2
`

type CountTireInstallationsParams struct {
	ParentID  int64
	CompanyID int64
}

func (q *Queries) CountTireInstallations(ctx context.Context, arg CountTireInstallationsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countTireInstallations, arg.ParentID, arg.CompanyID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createTireInstallation = `-- name: CreateTireInstallation :one
INSERT INTO tire_installation (
    tire_id, vehicle_id, position_code, install_date, odometer_at_install, tread_depth_at_install_32nds, psi_at_install, installed_by_id, notes
)
SELECT $1, $2, $3, $4, $5, $6, $7, $8, $9
WHERE EXISTS (SELECT 1 FROM tire WHERE id = $1 AND company_id = $10)
RETURNING id, vehicle_id, tire_id, position_code, install_date, odometer_at_install, tread_depth_at_install_32nds, psi_at_install, installed_by_id, notes
`

type CreateTireInstallationParams struct {
	ParentID                 int64
	VehicleID                int64
	PositionCode             string
	InstallDate              time.Time
	OdometerAtInstall        int32
	TreadDepthAtInstall32nds *int32
	PsiAtInstall             *decimal.Decimal
	InstalledByID            *int64
	Notes                    string
	CompanyID                int64
}

func (q *Queries) CreateTireInstallation(ctx context.Context, arg CreateTireInstallationParams) (TireInstallation, error) {
	row := q.db.QueryRow(ctx, createTireInstallation,
		arg.ParentID,
		arg.VehicleID,
		arg.PositionCode,
		arg.InstallDate,
		arg.OdometerAtInstall,
		arg.TreadDepthAtInstall32nds,
		arg.PsiAtInstall,
		arg.InstalledByID,
		arg.Notes,
		arg.CompanyID,
	)
	var i TireInstallation
	err := row.Scan(
		&i.ID,
		&i.VehicleID,
		&i.TireID,
		&i.PositionCode,
		&i.InstallDate,
		&i.OdometerAtInstall,
		&i.TreadDepthAtInstall32nds,
		&i.PsiAtInstall,
		&i.InstalledByID,
		&i.Notes,
	)
	return i, err
}

const deleteTireInstallation = `-- name: DeleteTireInstallation :exec
DELETE FROM tire_installation AS c USING tire p
WHERE c.id = $1 AND c.tire_id = $2 AND p.company_id = $3 AND p.id = c.tire_id
`

type DeleteTireInstallationParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) DeleteTireInstallation(ctx context.Context, arg DeleteTireInstallationParams) error {
	_, err := q.db.Exec(ctx, deleteTireInstallation, arg.ID, arg.ParentID, arg.CompanyID)
	return err
}

const getTireInstallation = `-- name: GetTireInstallation :one
SELECT c.id, c.vehicle_id, c.tire_id, c.position_code, c.install_date, c.odometer_at_install, c.tread_depth_at_install_32nds, c.psi_at_install, c.installed_by_id, c.notes FROM tire_installation c JOIN tire p ON p.id = c.tire_id
WHERE c.id = $1 AND c.tire_id = $2 AND p.company_id = $3
`

type GetTireInstallationParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) GetTireInstallation(ctx context.Context, arg GetTireInstallationParams) (TireInstallation, error) {
	row := q.db.QueryRow(ctx, getTireInstallation, arg.ID, arg.ParentID, arg.CompanyID)
	var i TireInstallation
	err := row.Scan(
		&i.ID,
		&i.VehicleID,
		&i.TireID,
		&i.PositionCode,
		&i.InstallDate,
		&i.OdometerAtInstall,
		&i.TreadDepthAtInstall32nds,
		&i.PsiAtInstall,
		&i.InstalledByID,
		&i.Notes,
	)
	return i, err
}

const listTireInstallations = `-- name: ListTireInstallations :many
SELECT c.id, c.vehicle_id, c.tire_id, c.position_code, c.install_date, c.odometer_at_install, c.tread_depth_at_install_32nds, c.psi_at_install, c.installed_by_id, c.notes FROM tire_installation c JOIN tire p ON p.id = c.tire_id
WHERE c.tire_id = $1 AND p.company_id = $2
ORDER BY c.install_date DESC, c.id LIMIT $4 OFFSET $3
`

type ListTireInstallationsParams struct {
	ParentID  int64
	CompanyID int64
	Off       int32
	Lim       int32
}

func (q *Queries) ListTireInstallations(ctx context.Context, arg ListTireInstallationsParams) ([]TireInstallation, error) {
	rows, err := q.db.Query(ctx, listTireInstallations,
		arg.ParentID,
		arg.CompanyID,
		arg.Off,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TireInstallation{}
	for rows.Next() {
		var i TireInstallation
		if err := rows.Scan(
			&i.ID,
			&i.VehicleID,
			&i.TireID,
			&i.PositionCode,
			&i.InstallDate,
			&i.OdometerAtInstall,
			&i.TreadDepthAtInstall32nds,
			&i.PsiAtInstall,
			&i.InstalledByID,
			&i.Notes,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const tireHasInstallation = `-- name: TireHasInstallation :one
SELECT EXISTS(SELECT 1 FROM tire_installation WHERE tire_id = $1)::boolean AS installed
`

func (q *Queries) TireHasInstallation(ctx context.Context, tireID int64) (bool, error) {
	row := q.db.QueryRow(ctx, tireHasInstallation, tireID)
	var installed bool
	err := row.Scan(&installed)
	return installed, err
}

const tirePositionOccupied = `-- name: TirePositionOccupied :one
SELECT EXISTS(SELECT 1 FROM tire_installation WHERE vehicle_id = $1 AND position_code = $2)::boolean AS occupied
`

type TirePositionOccupiedParams struct {
	VehicleID    int64
	PositionCode string
}

func (q *Queries) TirePositionOccupied(ctx context.Context, arg TirePositionOccupiedParams) (bool, error) {
	row := q.db.QueryRow(ctx, tirePositionOccupied, arg.VehicleID, arg.PositionCode)
	var occupied bool
	err := row.Scan(&occupied)
	return occupied, err
}

const updateTireInstallation = `-- name: UpdateTireInstallation :one
UPDATE tire_installation AS c SET vehicle_id = $1, position_code = $2, install_date = $3, odometer_at_install = $4, tread_depth_at_install_32nds = $5, psi_at_install = $6, installed_by_id = $7, notes = $8
FROM tire p
WHERE c.id = $9 AND c.tire_id = $10 AND p.company_id = $11 AND p.id = c.tire_id
RETURNING c.id, c.vehicle_id, c.tire_id, c.position_code, c.install_date, c.odometer_at_install, c.tread_depth_at_install_32nds, c.psi_at_install, c.installed_by_id, c.notes
`

type UpdateTireInstallationParams struct {
	VehicleID                int64
	PositionCode             string
	InstallDate              time.Time
	OdometerAtInstall        int32
	TreadDepthAtInstall32nds *int32
	PsiAtInstall             *decimal.Decimal
	InstalledByID            *int64
	Notes                    string
	ID                       int64
	ParentID                 int64
	CompanyID                int64
}

func (q *Queries) UpdateTireInstallation(ctx context.Context, arg UpdateTireInstallationParams) (TireInstallation, error) {
	row := q.db.QueryRow(ctx, updateTireInstallation,
		arg.VehicleID,
		arg.PositionCode,
		arg.InstallDate,
		arg.OdometerAtInstall,
		arg.TreadDepthAtInstall32nds,
		arg.PsiAtInstall,
		arg.InstalledByID,
		arg.Notes,
		arg.ID,
		arg.ParentID,
		arg.CompanyID,
	)
	var i TireInstallation
	err := row.Scan(
		&i.ID,
		&i.VehicleID,
		&i.TireID,
		&i.PositionCode,
		&i.InstallDate,
		&i.OdometerAtInstall,
		&i.TreadDepthAtInstall32nds,
		&i.PsiAtInstall,
		&i.InstalledByID,
		&i.Notes,
	)
	return i, err
}
