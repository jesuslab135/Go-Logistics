// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: purchase_order_line_item.sql

package gen

import (
	"context"
	"time"

	"github.com/shopspring/decimal"
)

const countPurchaseOrderLineItems = `-- name: CountPurchaseOrderLineItems :one
SELECT count(*) FROM purchase_order_line_item c JOIN purchase_order p ON p.id = c.purchase_order_id WHERE c.purchase_order_id = $1 AND p.company_id = $2
`

type CountPurchaseOrderLineItemsParams struct {
	ParentID  int64
	CompanyID int64
}

func (q *Queries) CountPurchaseOrderLineItems(ctx context.Context, arg CountPurchaseOrderLineItemsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countPurchaseOrderLineItems, arg.ParentID, arg.CompanyID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createPurchaseOrderLineItem = `-- name: CreatePurchaseOrderLineItem :one
INSERT INTO purchase_order_line_item (
    purchase_order_id, part_id, quantity, total_received, unit_cost, subtotal, position, created_at, updated_at
)
SELECT $1, $2, $3, $4, $5, $6, $7, $8, $9
WHERE EXISTS (SELECT 1 FROM purchase_order WHERE id = $1 AND company_id = $10)
RETURNING id, purchase_order_id, part_id, quantity, total_received, unit_cost, subtotal, position, created_at, updated_at
`

type CreatePurchaseOrderLineItemParams struct {
	ParentID      int64
	PartID        int64
	Quantity      decimal.Decimal
	TotalReceived decimal.Decimal
	UnitCost      decimal.Decimal
	Subtotal      decimal.Decimal
	Position      int32
	CreatedAt     time.Time
	UpdatedAt     time.Time
	CompanyID     int64
}

func (q *Queries) CreatePurchaseOrderLineItem(ctx context.Context, arg CreatePurchaseOrderLineItemParams) (PurchaseOrderLineItem, error) {
	row := q.db.QueryRow(ctx, createPurchaseOrderLineItem,
		arg.ParentID,
		arg.PartID,
		arg.Quantity,
		arg.TotalReceived,
		arg.UnitCost,
		arg.Subtotal,
		arg.Position,
		arg.CreatedAt,
		arg.UpdatedAt,
		arg.CompanyID,
	)
	var i PurchaseOrderLineItem
	err := row.Scan(
		&i.ID,
		&i.PurchaseOrderID,
		&i.PartID,
		&i.Quantity,
		&i.TotalReceived,
		&i.UnitCost,
		&i.Subtotal,
		&i.Position,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deletePurchaseOrderLineItem = `-- name: DeletePurchaseOrderLineItem :exec
DELETE FROM purchase_order_line_item AS c USING purchase_order p
WHERE c.id = $1 AND c.purchase_order_id = $2 AND p.company_id = $3 AND p.id = c.purchase_order_id
`

type DeletePurchaseOrderLineItemParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) DeletePurchaseOrderLineItem(ctx context.Context, arg DeletePurchaseOrderLineItemParams) error {
	_, err := q.db.Exec(ctx, deletePurchaseOrderLineItem, arg.ID, arg.ParentID, arg.CompanyID)
	return err
}

const getPurchaseOrderLineItem = `-- name: GetPurchaseOrderLineItem :one
SELECT c.id, c.purchase_order_id, c.part_id, c.quantity, c.total_received, c.unit_cost, c.subtotal, c.position, c.created_at, c.updated_at FROM purchase_order_line_item c JOIN purchase_order p ON p.id = c.purchase_order_id
WHERE c.id = $1 AND c.purchase_order_id = $2 AND p.company_id = $3
`

type GetPurchaseOrderLineItemParams struct {
	ID        int64
	ParentID  int64
	CompanyID int64
}

func (q *Queries) GetPurchaseOrderLineItem(ctx context.Context, arg GetPurchaseOrderLineItemParams) (PurchaseOrderLineItem, error) {
	row := q.db.QueryRow(ctx, getPurchaseOrderLineItem, arg.ID, arg.ParentID, arg.CompanyID)
	var i PurchaseOrderLineItem
	err := row.Scan(
		&i.ID,
		&i.PurchaseOrderID,
		&i.PartID,
		&i.Quantity,
		&i.TotalReceived,
		&i.UnitCost,
		&i.Subtotal,
		&i.Position,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listPurchaseOrderLineItems = `-- name: ListPurchaseOrderLineItems :many
SELECT c.id, c.purchase_order_id, c.part_id, c.quantity, c.total_received, c.unit_cost, c.subtotal, c.position, c.created_at, c.updated_at FROM purchase_order_line_item c JOIN purchase_order p ON p.id = c.purchase_order_id
WHERE c.purchase_order_id = $1 AND p.company_id = $2
ORDER BY c.position, c.id LIMIT $4 OFFSET $3
`

type ListPurchaseOrderLineItemsParams struct {
	ParentID  int64
	CompanyID int64
	Off       int32
	Lim       int32
}

func (q *Queries) ListPurchaseOrderLineItems(ctx context.Context, arg ListPurchaseOrderLineItemsParams) ([]PurchaseOrderLineItem, error) {
	rows, err := q.db.Query(ctx, listPurchaseOrderLineItems,
		arg.ParentID,
		arg.CompanyID,
		arg.Off,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PurchaseOrderLineItem{}
	for rows.Next() {
		var i PurchaseOrderLineItem
		if err := rows.Scan(
			&i.ID,
			&i.PurchaseOrderID,
			&i.PartID,
			&i.Quantity,
			&i.TotalReceived,
			&i.UnitCost,
			&i.Subtotal,
			&i.Position,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePurchaseOrderLineItem = `-- name: UpdatePurchaseOrderLineItem :one
UPDATE purchase_order_line_item AS c SET part_id = $1, quantity = $2, total_received = $3, unit_cost = $4, subtotal = $5, position = $6, updated_at = $7
FROM purchase_order p
WHERE c.id = $8 AND c.purchase_order_id = $9 AND p.company_id = $10 AND p.id = c.purchase_order_id
RETURNING c.id, c.purchase_order_id, c.part_id, c.quantity, c.total_received, c.unit_cost, c.subtotal, c.position, c.created_at, c.updated_at
`

type UpdatePurchaseOrderLineItemParams struct {
	PartID        int64
	Quantity      decimal.Decimal
	TotalReceived decimal.Decimal
	UnitCost      decimal.Decimal
	Subtotal      decimal.Decimal
	Position      int32
	UpdatedAt     time.Time
	ID            int64
	ParentID      int64
	CompanyID     int64
}

func (q *Queries) UpdatePurchaseOrderLineItem(ctx context.Context, arg UpdatePurchaseOrderLineItemParams) (PurchaseOrderLineItem, error) {
	row := q.db.QueryRow(ctx, updatePurchaseOrderLineItem,
		arg.PartID,
		arg.Quantity,
		arg.TotalReceived,
		arg.UnitCost,
		arg.Subtotal,
		arg.Position,
		arg.UpdatedAt,
		arg.ID,
		arg.ParentID,
		arg.CompanyID,
	)
	var i PurchaseOrderLineItem
	err := row.Scan(
		&i.ID,
		&i.PurchaseOrderID,
		&i.PartID,
		&i.Quantity,
		&i.TotalReceived,
		&i.UnitCost,
		&i.Subtotal,
		&i.Position,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
