-- 000004_backfill_employee_companies.down.sql
-- Intentionally empty: backfilled rows are indistinguishable from memberships
-- created through the API, so removing them would delete legitimate data.

SELECT 1;
